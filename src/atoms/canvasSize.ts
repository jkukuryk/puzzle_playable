import { atom } from 'recoil';
export const canvasWidthAtom = atom({ key: 'canvasWidthAtom', default: 0 });
export const canvasHeightAtom = atom({ key: 'canvasHeightAtom', default: 0 });

export const gameViewWidthAtom = atom({ key: 'gameViewWidthAtom', default: 0 });
export const gameViewHeightAtom = atom({ key: 'gameViewHeightAtom', default: 0 });
